import { GenreForm } from "@/modules/genre/components/GenreForm.jsx";

/**
 * Página para la actualización de un género existente.
 * Renderiza el componente GenreForm configurado para actualización.
 * 
 * @returns {JSX.Element} La página de actualización de géneros.
 */
export const GenreUpdatePage = () => {

    return (
        <GenreForm />
    );
};