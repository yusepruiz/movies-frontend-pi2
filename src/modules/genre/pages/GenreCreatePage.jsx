import { GenreForm } from "@/modules/genre/components/GenreForm.jsx";

/**
 * Página para la creación de un nuevo género.
 * Renderiza el componente GenreForm en modo creación.
 * 
 * @returns {JSX.Element} La página de creación de géneros.
 */
export const GenreCreatePage = () => {
    return (
        <GenreForm />
    );
}