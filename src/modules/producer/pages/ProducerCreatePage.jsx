import { ProducerForm } from "@/modules/producer/components/ProducerForm";

/**
 * Página para la creación de un nuevo productor.
 * Renderiza el componente ProducerForm en modo creación.
 * 
 * @returns {JSX.Element} La página de creación de productores.
 */
export const ProducerCreatePage = () => {
    return (
        <ProducerForm />
    );
}