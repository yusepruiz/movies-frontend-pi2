import { TypeForm } from "@/modules/type/components/TypeForm.jsx";

/**
 * Página para la creación de un nuevo tipo de película.
 * Renderiza el componente TypeForm en modo creación.
 * 
 * @returns {JSX.Element} La página de creación de tipos.
 */
export const TypeCreatePage = () => {
    return (
        <TypeForm />
    );
}
