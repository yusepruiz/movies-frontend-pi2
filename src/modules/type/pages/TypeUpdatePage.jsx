import { TypeForm } from "@/modules/type/components/TypeForm.jsx";

/**
 * Página para la actualización de un tipo de película existente.
 * Renderiza el componente TypeForm configurado para actualización.
 * 
 * @returns {JSX.Element} La página de actualización de tipos.
 */
export const TypeUpdatePage = () => {
    return (
        <TypeForm />
    );
};
