import { MediaForm } from "@/modules/media/components/MediaForm";

/**
 * Página para la creación de un nuevo medio.
 * Renderiza el componente MediaForm configurado para creación.
 * 
 * @returns {JSX.Element} La página de creación de medios.
 */
export const MediaCreatePage = () => {

    return (
        <MediaForm />
    );
};
