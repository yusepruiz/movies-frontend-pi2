/**
 * Página de marcador de posición para la gestión de géneros.
 * 
 * @returns {JSX.Element} La página de géneros.
 */
export const GenderPage = () => {
    return (
        <div>GenderPage</div>
    )
}
